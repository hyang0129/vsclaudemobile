[project]
name = "vsclaudemobile"
version = "0.1.0"
description = "Mobile mirror of VSCode Claude Code extension"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "websockets>=12.0",
    "aiofiles>=23.0",
]

[tool.uv]
dev-dependencies = []
